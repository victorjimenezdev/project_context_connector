parameters:
  level: 6
  paths:
    - src
    - tests
  checkGenericClassInNonGenericObjectType: false
  treatPhpDocTypesAsCertain: false

  # Keep your local stubs so PHPStan knows about update_* functions without
  # requiring the Update module at analysis time.
  bootstrapFiles:
    - phpstan-stubs.php